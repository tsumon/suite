# Suite P2 backlog（2026-09-06）

权威：`design/INTERACTION-P2.md`（覆盖旧「本轮不做放大镜/取色/历史/副屏」）。  
源码：box `/workspace/windows-suite-app/`。Windows（tsumon）未开机 → **未编、未跑、UI 未绿**。  
**不做：** 代码签名。

## 状态

| # | 项 | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 更新通道 | **box 已落** | 关于：正式/预览 +「检查更新」；`UpdateChecker` 打 GitHub releases；占位 `tsumon/suite` →「尚未在本机启用」；无强制安装 |
| 2 | 崩溃/透明兜底 | **box 已落** | `CrashGuard` dirty-exit → 关效果 / 改悬浮 / 安全设置；运行中 TaskbarFx 连续失败自动关；进程拆分仅文档为 future |
| 3 | 截图历史 | **box 已落** | `History\` 下最多 30 / 7 天；托盘「截图历史」+ 设置入口；复制/钉/删/清空确认；磁盘文件、缩略图 OnLoad 释放 |
| 4 | 贴图管理 + 穿透键 | **box 已落** | 托盘「贴图管理」；默认 Ctrl+Shift+Q；托盘取消全部穿透；中键切换穿透 |
| 5 | 放大镜 | **box 已落** | 选区拖中 120×120 / 2×；设置「显示放大镜」默认开；不进工具栏 |
| 6 | 取色 | **box 已落** | F2 + 栏 `Pick`（识字旁）；复制 `#RRGGBB`；中文气泡 |
| 7 | 副屏网速 | **box 已落（best-effort）** | 设置默认关；`TryAttachToTray(Shell_SecondaryTrayWnd)`；真机验证要 Windows |
| 8 | 网速拖位 | **box 已落** | 按 P2：嵌入不可拖出；右键「改回桌面悬浮窗」；悬浮 Left/Top 仍写盘 |
| 9 | 浅色一键亚克力 | **box 已落** | 条件出现「改用亚克力并加深网速字」；不覆盖用户自选色 |
| 10 | 冒烟脚本 | **box 已落** | `scripts/smoke-suite.ps1` + `smoke-win.bat`；`SMOKE PASS/FAIL` |
| 11 | 日志开关 | **box 已落** | 关于「写入调试日志」默认关；`LocalAppData\Suite\logs`；打开文件夹 |

## 仍需 Windows

- 真机编 `Suite.App` / Native / TAP；跑 `scripts/smoke-suite.ps1`
- F1 栏 Pick、F2 取色、放大镜跟手、历史窗、贴图管理
- 崩溃降级气泡与安全设置路径
- 副屏 `Shell_SecondaryTrayWnd` 嵌入（Win11 现代栏可能失败——应说人话并跳过）
- 更新通道对真实 GitHub owner/repo（非占位）拉 releases

## 非目标

- 代码签名 / 公证
- 翻译、云同步、插件市场、CPU/GPU 监视、全桌面自动滚动
- 任务栏效果独立进程（P2 仅 fail-safe；拆分 future）
