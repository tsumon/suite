# Suite P1  backlog（2026-09-06）

Joe 点名：安装包、热键冲突、多屏、OCR、长截图滚动、轻动效。  
权威源码：box `/workspace/windows-suite-app/`。Windows（tsumon）未开机 → **未编、未跑、UI 未绿**。

## 优先级（先做 → 后做）

| # | 项 | 为何排这 | 工作量 | 依赖 Windows 开机 | DoD |
| --- | --- | --- | --- | --- | --- |
| 1 | **热键冲突处理** | 高价值；F1 常被占用；纯 box 可写完 | S | 否（验证要） | 注册失败有中文原因；设置页状态 + 建议 F 键；「试用下一可用键」探测 F1–F12/字母；默认仍 F1；不循环抢键 |
| 2 | **多屏审计 / 硬化** | 选区/钉图/DPI 易错；多数可 box 审 + 修 | M | 否（双屏验证要） | 审计 Capture 每屏 overlay、钉图落点、网速主栏、TaskbarFx 多栏；修明确 bug；残余写入本文件 |
| 3 | **安装包骨架** | 发版前置；Inno 脚本可在 Linux 起草 | S | 是（编 iss / 签名） | `installer/Suite.iss` + `PORTABLE.md`；Program Files / 开始菜单 / 可选登录启动；旁路 TAP/Native DLL；GPL 提示；**不声称已打出安装包** |
| 4 | **轻动效** | Operate 轻量；钉图光环已有 | XS | 否（观感要） | 钉图 glow 保留；设置窗可选 ≤120ms Opacity 淡入；截图工具栏松手零延迟；DESIGN.md 对齐 |
| 5 | **OCR** | WinRT `Windows.Media.Ocr`；先契约 | M | 是（真 OCR） | `design/OCR-SCROLL-SPEC.md` v1.1；`WindowsOcrService` + 栏「识字」；真机验语言包 |
| 6 | **滚动长截图** | 难；Win11 滚轮/合成坑多 | L | 是 | 合约 + box `ScrollCaptureService` v1；托盘入口；真机矩阵另开 |

## 本轮已落（box）

- [x] 本 backlog
- [x] 热键冲突代码（`HotkeyCopy` / `NativeHotKey` / `HotKeyHost` / `AppController` / 设置页）
- [x] 安装包骨架（`installer/`）
- [x] 多屏：`ResolveMonitor` 改最大重叠；贴图剪贴板按指针监视器 DPI；审计笔记见下
- [x] `design/OCR-SCROLL-SPEC.md` + OCR stub 接口
- [x] 设置窗 120ms 淡入；DESIGN.md Don't 修订
- [x] `BUILD-HANDOFF-20260906.md` 附录指向本文件
- [x] OCR：`WindowsOcrService` + 松手栏识字（DI 回退 Null）；见 INTERACTION-P1
- [x] 滚动长截图：`ScrollCaptureService` + 托盘/设置入口（不改 F1）
- [x] 浅色+透明：**内联提示**（不强制改 Clear）；关于「恢复默认设置」
- [x] 内存：会话结束释帧 / Bake 缓存 / 滚动中间缓冲释放
- [x] `design/INTERACTION-P1.md`；`plans/BOX-DONE-20260906.md`

## 多屏审计笔记（2026-09-06）

| 区域 | 结论 | 动作 |
| --- | --- | --- |
| Capture overlays | 每监视器一扇 `RegionOverlayWindow`；坐标用该屏 Bounds+DPI | 保持 |
| Annotation 窗 | 只落在**最大重叠**屏（曾取枚举第一屏，跨屏选区会偏） | **已修** `CaptureService.ResolveMonitor` / `CaptureUx.IndexOfLargestOverlap` |
| 跨屏选区标注 | 标注窗仍单屏；跨屏裁切靠 `RegionCropper` 拼帧 | 残余：标注铬不跨屏绘制（可接受；另卡） |
| Pin 落点 | 用选区 DPI→DIP；剪贴板钉曾用系统 DPI | **已修** `PinNative.DpiNearCursor` |
| NetSpeed embed | **仅主栏** `Shell_TrayWnd`（设计如此） | 残余：副屏任务栏无网速槽 |
| TaskbarFx | Classic SWCA：`CollectTrays` 含 Secondary；Win11 TAP 注入主 explorer（副栏同进程） | 残余：重启探测只盯主栏 HWND |

## 非目标（整份 backlog）

- 引入 TrafficMonitor 源码 / 逆向 Snipaste
- 在 Debian 声称安装包已编出或 UI 已绿
- 作品集风弹跳 / 长动效；工具栏入场动画

## 滚动长截图 — Win11 限制（文档，待真机）

| 坑 | 说明 | v1 态度 |
| --- | --- | --- |
| Chromium / Electron 自绘 | 常不吃 `WM_MOUSEWHEEL` / 客户区 BitBlt 与合成不一致 | 失败句「这个窗口暂时滚不动长图。」 |
| 平滑滚动 | 帧间差分抖动，重叠估计易偏 | 无变化 N 帧停；可接受裁切缝 |
| DPI 混合 | 跨屏 DPI 客户区坐标 | 单 HWND；不跨屏滚 |
| 合成 / 分层窗 | PrintWindow 仍可能黑/旧帧 | best-effort；不注入 |
| 超时 | 长页面 | 上限高度 + 总时长；已拼部分若几乎无滚则失败句 |
